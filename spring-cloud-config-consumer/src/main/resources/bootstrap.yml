server:
  port: 8081
#????????2.1
#spring:
#  application:
#    name: testConfig #???????
#  cloud:
#    config:
#      uri: http://127.0.0.1:8080 # ???config?????url
#      label: main # ????????????
#      profile: dev # ????????????(dev | demo | prod)

#????????2.2
spring:
  application:
    name: spring-cloud-config-consumer #???????
  cloud:
    config:
      uri: http://127.0.0.1:8080 # ???config?????url
      profile: dev # ????????????(dev | demo | prod)